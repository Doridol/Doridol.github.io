let num= 150;

function setup() {
  createCanvas(300, 300);
  background(230);
  //ellipse(num, num, 30, 30)
  //text("hello", 0, 10); // corner = left bottom
  //text("hello", 0, 0, 10, 100); // corner = left top
  //text("hello", 136.5, 155)
  //text("hello my name is sanghyuck", 10, 10, 100, 30)
  //칸이 작다 -> 문자열이 자동으로 줄변환

  textSize();
  text("안녕", 100, 140)
}

function draw() {
 // ellipse(num, 150, 30, 30);
 // num += 10;
}